#include <stdio.h>
#include <stdlib.h>
int topla(int x,int y)
{
    int sonuc=x+y;
    return sonuc;
}
int carp(int s1,int s2)
{
    int sonuc=s1*s2;
    return sonuc;
}
int karekok(int x int y)
{
    int sonuc =sqrt
}

int main()
{   int a,b;
    printf("2 sayi giriniz:");
    scanf("%d%d",&a,&b);
    printf("toplam=%d",topla(a,b));
printf("carp=%d",carp(a,b));
    return 0;
}

